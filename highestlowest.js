let prices = [2, 8];
let lowest = highest = newLow = highLow = 0;

// loop through prices.
// low/highest prices
// current prices
// if new low price is lower then low price start new comparison.
console.log(lowest + " " + highest + " " + newLow + " " + highLow);
